---
title: "Página del carrito de compras de repuestos en LUVEN"
sidebarTitle: "Carrito"
description: "El carrito de LUVEN muestra los repuestos seleccionados antes del pedido. Requiere sesión activa y muestra el resumen con total y botón de checkout."
---

El carrito de LUVEN (`Frontend/carito.php`) es la página donde el usuario revisa todos los repuestos que ha seleccionado antes de completar su pedido. La página detecta si el usuario ha iniciado sesión y ajusta la interfaz en consecuencia. El resumen lateral muestra el total acumulado y el botón de checkout, que permanece deshabilitado mientras el carrito esté vacío.

---

## URL de acceso

```text
Frontend/carito.php
```

---

## Requisito de sesión

El carrito verifica la existencia de la sesión PHP al cargar:

```php Frontend/carito.php
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}
```

La variable `$_SESSION['usuario_id']` es la que determina si se muestra el estado autenticado o el estado vacío con botones de acceso.

### Acceso sin sesión desde el icono del carrito

Si un usuario no autenticado hace clic en el icono del carrito desde cualquier página de la tienda o de servicios, es redirigido al login con un mensaje de alerta y el parámetro de retorno:

```text
Frontend/login.php?redirigir=carrito&error=1
```

Esto activa la alerta informativa en el formulario de login: `"Para continuar es necesario que se registre o inicie sesión."`.

---

## Estado vacío del carrito

La implementación actual de `Frontend/carito.php` es un scaffold de interfaz: `$carrito_vacio` está fijado en `true` de forma explícita en el código, por lo que la pantalla de carrito vacío siempre se muestra independientemente de si el usuario ha agregado productos:

```php Frontend/carito.php
// Simulamos que el carrito está vacío para ver la alerta o el diseño base
$carrito_vacio = true;
```

Cuando `$carrito_vacio` es `true`, se muestra una pantalla centrada con icono, mensaje y dos botones de acción:

```php Frontend/carito.php
<?php if ($carrito_vacio): ?>
    <div class="carrito-estado-vacio">
        <div class="icon-wrap">
            <i class="fa-solid fa-cart-shopping"></i>
        </div>
        <h3>El carrito de compras está vacío</h3>
        <p>Agrega tus repuestos o artículos favoritos.</p>

        <div class="carrito-vacio-botones">
            <a href="login.php?redirigir=carrito&error=1" class="btn-temu-naranja">
                Iniciar sesión / Registrarse
            </a>
            <a href="../modulos/tienda/productos.php" class="btn-temu-blanco">
                Comienza a comprar
            </a>
        </div>
    </div>
<?php endif; ?>
```

| Botón | Destino | Estilo |
|---|---|---|
| **Iniciar sesión / Registrarse** | `login.php?redirigir=carrito&error=1` | Naranja (primario) |
| **Comienza a comprar** | `../modulos/tienda/productos.php` | Blanco (secundario) |

---

## Layout de dos columnas

La página define un layout de dos columnas para cuando el carrito tenga productos. Actualmente la columna de productos no se activa porque `$carrito_vacio = true` está codificado de forma fija:

### Columna izquierda — Lista de productos (scaffold)

La rama `else` del condicional `$carrito_vacio` contiene el contenedor `.carrito-tarjeta-item` vacío, reservado para cuando se implemente la lógica completa de agregar y eliminar productos del carrito.

### Columna derecha — Resumen del pedido

El panel lateral muestra el resumen del pedido, el total calculado y el botón de confirmación:

```php Frontend/carito.php
<div class="carrito-resumen-card">
    <h3>Resumen del pedido</h3>

    <div class="resumen-fila-total">
        <span>Total</span>
        <strong>S/ 0.00</strong>
    </div>

    <p class="resumen-nota">Consulta el monto final de tu pago real.</p>

    <button class="btn-temu-orden" disabled>Hacer pedido (0)</button>

    <div class="carrito-confianza-lista">
        <div class="confianza-item">
            <i class="fa-solid fa-lock text-verde"></i>
            <span>No se te cobrará hasta que revises este pedido en la página siguiente.</span>
        </div>
        <div class="confianza-item">
            <i class="fa-solid fa-shield-halved text-verde"></i>
            <span><strong>Opciones de pago seguro:</strong>
                LUVEN se compromete a proteger tu información de pago.</span>
        </div>
    </div>
</div>
```

---

## Elementos de confianza

La página muestra dos indicadores de seguridad y condiciones de envío para generar confianza en el usuario antes del checkout:

<CardGroup cols={2}>
  <Card title="Envío incluido" icon="truck-fast" href="/user/tienda">
    Un banner en la parte superior informa que el envío está disponible en todas las compras de repuestos y piezas seleccionadas.
  </Card>
  <Card title="Pago seguro" icon="lock" href="/user/carrito">
    El resumen lateral incluye una nota de seguridad: el usuario no será cobrado hasta revisar el pedido en la página siguiente. LUVEN declara proteger la información de pago del cliente.
  </Card>
</CardGroup>

---

## Flujo de compra

<Steps>
  <Step title="Agregar productos desde la tienda">
    El usuario navega a `Frontend/productos.php`, explora el catálogo y hace clic en **Ver** para ver el detalle del producto. Requiere sesión activa para agregar al carrito.
  </Step>
  <Step title="Revisar el carrito">
    En `Frontend/carito.php`, el usuario verá la lista de productos seleccionados y el total calculado en el resumen lateral.
  </Step>
  <Step title="Confirmar el pedido">
    Al hacer clic en el botón **"Hacer pedido"** (habilitado cuando hay al menos un producto), el usuario pasa a la página de confirmación y pago.
  </Step>
</Steps>

---

<Note>
  La implementación actual de `Frontend/carito.php` corresponde al scaffold de interfaz del carrito: `$carrito_vacio = true` está codificado de forma fija y la lógica de agregar, eliminar y persistir productos del carrito no está implementada todavía. El campo `activo = 1` de la tabla `productos` garantiza que solo los productos publicados por el administrador sean visibles en la tienda.
</Note>
